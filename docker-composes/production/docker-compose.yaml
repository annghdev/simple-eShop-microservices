services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
      POSTGRES_INITDB_ARGS: --auth-host=scram-sha-256 --auth-local=scram-sha-256
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/01-create-databases.sql:/docker-entrypoint-initdb.d/01-create-databases.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d postgres"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 30
    restart: unless-stopped
    networks:
      - eshop

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:-redis}"]
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks:
      - eshop

  rabbitmq:
    image: rabbitmq:4.1-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USERNAME:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    ports:
      - "${RABBITMQ_MANAGEMENT_PORT:-15672}:15672"
      - "${RABBITMQ_METRICS_PORT:-15692}:15692"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks:
      - eshop

  apigateway:
    build:
      context: ..
      dockerfile: src/APIGateway/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__authdb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=authdb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
      ReverseProxy__Clusters__catalog-cluster__Destinations__catalog-api__Address: http://catalog:8080
      ReverseProxy__Clusters__inventory-cluster__Destinations__inventory-api__Address: http://inventory:8080
      ReverseProxy__Clusters__order-cluster__Destinations__order-api__Address: http://order:8080
      ReverseProxy__Clusters__payment-cluster__Destinations__payment-api__Address: http://payment:8080
      ReverseProxy__Clusters__shipping-cluster__Destinations__shipping-api__Address: http://shipping:8080
    ports:
      - "${APIGATEWAY_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      payment:
        condition: service_started
      shipping:
        condition: service_started
    restart: unless-stopped
    networks:
      - eshop

  catalog:
    build:
      context: ..
      dockerfile: src/services/Catalog/Catalog.API/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__catalogdb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=catalogdb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  inventory:
    build:
      context: ..
      dockerfile: src/services/Inventory/Inventory.API/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__order: http://order:8082
      ConnectionStrings__inventorydb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=inventorydb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  order:
    build:
      context: ..
      dockerfile: src/services/Order/Order.API/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__inventory: http://inventory:8082
      ConnectionStrings__orderdb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=orderdb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  payment:
    build:
      context: ..
      dockerfile: src/services/Payment/Payment.API/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__paymentdb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=paymentdb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  shipping:
    build:
      context: ..
      dockerfile: src/services/Shipping/Shipping.API/Dockerfile
    environment:
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Production}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__shippingdb: Host=postgres;Port=5432;Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-postgres};Database=shippingdb
      ConnectionStrings__redis: redis:6379,password=${REDIS_PASSWORD:-redis}
      ConnectionStrings__rabbitmq: amqp://${RABBITMQ_USERNAME:-guest}:${RABBITMQ_PASSWORD:-guest}@rabbitmq:5672
      JAEGER_OTLP_ENDPOINT: http://jaeger:4317
      TEMPO_OTLP_ENDPOINT: http://tempo:4319
      LOKI_ENDPOINT: http://loki:3100
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  prometheus:
    image: prom/prometheus:v3.3.1
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=7d
      - --web.enable-lifecycle
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./development/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    depends_on:
      - apigateway
      - catalog
      - inventory
      - order
      - payment
      - shipping
      - postgres-exporter
      - redis-exporter
      - rabbitmq
    restart: unless-stopped
    networks:
      - eshop

  loki:
    image: grafana/loki:3.5.0
    command: -config.file=/etc/loki/loki-config.yml
    ports:
      - "${LOKI_PORT:-3100}:3100"
    volumes:
      - ./monitoring/loki/loki-config.yml:/etc/loki/loki-config.yml:ro
      - loki_data:/loki
    restart: unless-stopped
    networks:
      - eshop

  jaeger:
    image: jaegertracing/all-in-one:1.67.0
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "${JAEGER_UI_PORT:-16686}:16686"
      - "${JAEGER_OTLP_GRPC_PORT:-4317}:4317"
      - "${JAEGER_OTLP_HTTP_PORT:-4318}:4318"
    restart: unless-stopped
    networks:
      - eshop

  tempo:
    image: grafana/tempo:2.8.2
    command: -config.file=/etc/tempo/tempo.yml
    ports:
      - "${TEMPO_PORT:-3200}:3200"
      - "${TEMPO_OTLP_GRPC_PORT:-4319}:4319"
    volumes:
      - ./monitoring/tempo/tempo.yml:/etc/tempo/tempo.yml:ro
      - tempo_data:/var/tempo
    restart: unless-stopped
    networks:
      - eshop

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.17.1
    environment:
      DATA_SOURCE_URI: postgres:5432/postgres?sslmode=disable
      DATA_SOURCE_USER: ${POSTGRES_USER:-postgres}
      DATA_SOURCE_PASS: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_EXPORTER_PORT:-9187}:9187"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  redis-exporter:
    image: oliver006/redis_exporter:v1.72.0
    command:
      - --redis.addr=redis://:${REDIS_PASSWORD:-redis}@redis:6379
    ports:
      - "${REDIS_EXPORTER_PORT:-9121}:9121"
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - eshop

  grafana:
    image: grafana/grafana:12.0.2
    depends_on:
      - prometheus
      - loki
      - jaeger
      - tempo
      - postgres-exporter
      - redis-exporter
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: "https://storage.googleapis.com/integration-artifacts/grafana-exploretraces-app/grafana-exploretraces-app-latest.zip;grafana-traces-app,https://storage.googleapis.com/integration-artifacts/grafana-lokiexplore-app/release/main/any/grafana-lokiexplore-app-main.zip;grafana-lokiexplore-app"
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    restart: unless-stopped
    networks:
      - eshop

networks:
  eshop:
    driver: bridge

volumes:
  postgres_data:
  prometheus_data:
  loki_data:
  tempo_data:
  grafana_data:
